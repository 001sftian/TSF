[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "weakpde-rl"
version = "0.1.0"
description = "Dependency-free weak-form RL/CEM sparse PDE discovery toolkit"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["weakpde_rl*"]
